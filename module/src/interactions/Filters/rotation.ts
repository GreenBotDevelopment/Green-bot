import { Command } from "../../abstract/QuickCommand";;export default class Rotation extends Command {
get name(){return"rotation"}get description(){return"Enables/disables the rotation filter"}get category(){return"Filters"}get checks(){return{voice:true,dispatcher:true,channel:true,dj:true,premium:true}}async run({ctx:e}){return e.dispatcher.player.filters.rotation?(e.dispatcher.player.clearFilters(),e.successMessage("⏱ Disabling the `Rotation` filter to the current song...")):(e.dispatcher.player.setRotation({rotationHz:0},e.dispatcher.player),e.successMessage("⏱ Enabling the `Rotation` filter to the current song..."))}}